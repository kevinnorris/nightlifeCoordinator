import 'es6-promise/auto';
import 'isomorphic-fetch';

// Actions

// Initial State
const initialState = {
  isFetching: false,
  results: null,
  token: '',
};

// Reducer

// Action Creators
export const getAccessToken = () => (
  return (dispatch) => {

  }
);
